# Test layer triage

Which layer owns which specified scenario. Generated by `tools/layer-triage` from
`openspec/specs/**/spec.md` and `tools/layer-triage/assignments.json` — do not edit by hand;
edit the assignments and re-run.

The `automated-testing` capability requires each scenario to be verified at the layer that can
observe its outcome most directly, and does not require a scenario observable at several layers
to appear at all of them. This table is where that call is recorded, with a reason, so an
omission is reviewable rather than invisible.

| Layer | Meaning |
|---|---|
| Backend integration | A rule the server enforces regardless of which client calls it |
| Frontend unit / component | What the interface shows, enables, or blocks before contacting the server |
| End-to-end | Spans browser, proxy, server, and database together |
| Load and capacity (not built) | Specified, deliberately outside the gated run |
| Verified by the tooling itself | The test machinery is its own subject |

## Totals

| Layer | Scenarios |
|---|---:|
| Backend integration | 185 |
| Frontend unit / component | 72 |
| End-to-end | 9 |
| Load and capacity (not built) | 4 |
| Verified by the tooling itself | 21 |
| **total** | **291** |

## attachments

18 of 33 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `attachments/uploading-attachments/uploading-a-file` | Backend integration | Upload acceptance and the membership and ban checks around it are enforced server-side on the multipart request. |
| `attachments/uploading-attachments/uploading-several-files-at-once` | Backend integration | Upload acceptance and the membership and ban checks around it are enforced server-side on the multipart request. |
| `attachments/uploading-attachments/non-member-upload` | Backend integration | Upload acceptance and the membership and ban checks around it are enforced server-side on the multipart request. |
| `attachments/uploading-attachments/banned-uploader` | Backend integration | Upload acceptance and the membership and ban checks around it are enforced server-side on the multipart request. |
| `attachments/uploading-attachments/empty-file` | Backend integration | Upload acceptance and the membership and ban checks around it are enforced server-side on the multipart request. |
| `attachments/upload-by-paste/pasting-an-image` | Frontend unit / component | Reading an image off the clipboard and handing it to the uploader is composer behaviour; the server sees an ordinary upload. |
| `attachments/size-limits-by-kind/oversized-image` | Backend integration | The per-kind byte ceiling is refused by the server regardless of what the client allows through. |
| `attachments/size-limits-by-kind/oversized-file` | Backend integration | The per-kind byte ceiling is refused by the server regardless of what the client allows through. |
| `attachments/attachment-metadata/original-name-preserved` | Backend integration | What is persisted alongside the bytes — original name, comment — is server state. |
| `attachments/attachment-metadata/comment-on-an-attachment` | Backend integration | What is persisted alongside the bytes — original name, comment — is server state. |
| `attachments/attachment-metadata/metadata-display` | Frontend unit / component | How name, size, and kind are laid out in the message row is rendering. |
| `attachments/attachment-metadata/voice-message-metadata-display` | Frontend unit / component | Duration and the voice-message treatment in the message row are rendering. |
| `attachments/inline-image-rendering/image-preview` | Frontend unit / component | Whether an attachment renders as a preview, a fallback, or a file chip is decided in the component. |
| `attachments/inline-image-rendering/preview-fails` | Frontend unit / component | Whether an attachment renders as a preview, a fallback, or a file chip is decided in the component. |
| `attachments/inline-image-rendering/other-file-kinds` | Frontend unit / component | Whether an attachment renders as a preview, a fallback, or a file chip is decided in the component. |
| `attachments/download-access-control/member-downloads` | Backend integration | Who may fetch the bytes is re-evaluated by the server on every download request. |
| `attachments/download-access-control/non-member-downloads` | Backend integration | Who may fetch the bytes is re-evaluated by the server on every download request. |
| `attachments/download-access-control/uploader-who-lost-access` | Backend integration | Who may fetch the bytes is re-evaluated by the server on every download request. |
| `attachments/download-access-control/unknown-attachment` | Backend integration | Who may fetch the bytes is re-evaluated by the server on every download request. |
| `attachments/attachment-storage-and-persistence/file-survives-loss-of-access` | Backend integration | Where the bytes land under the uploads root, and that they outlive the requester's access, is server-side storage behaviour. |
| `attachments/attachment-storage-and-persistence/storage-layout` | Backend integration | Where the bytes land under the uploads root, and that they outlive the requester's access, is server-side storage behaviour. |
| `attachments/voice-attachment-transcription/voice-attachment-gets-a-transcript` | Backend integration | Transcription is queued and persisted server-side; the outcome is observable on the attachment record. |
| `attachments/voice-attachment-transcription/transcription-fails-for-a-voice-attachment` | Backend integration | Transcription is queued and persisted server-side; the outcome is observable on the attachment record. |
| `attachments/voice-attachment-transcription/transcript-shown-alongside-playback` | Frontend unit / component | Where the transcript appears relative to the player is rendering. |
| `attachments/transcription-eligibility-length-limit/long-voice-attachment-uploads-without-a-transcript` | Backend integration | The server decides whether a clip is short enough to queue, and accepts the upload either way. |
| `attachments/recording-a-voice-message/recording-and-sending-a-voice-message` | Frontend unit / component | Recording is a MediaRecorder interaction in the composer; only the resulting upload reaches the server. |
| `attachments/recording-a-voice-message/recording-in-progress` | Frontend unit / component | Recording is a MediaRecorder interaction in the composer; only the resulting upload reaches the server. |
| `attachments/recording-a-voice-message/recording-reaches-the-length-limit` | Frontend unit / component | Recording is a MediaRecorder interaction in the composer; only the resulting upload reaches the server. |
| `attachments/recording-a-voice-message/microphone-unavailable-while-recording` | Frontend unit / component | Recording is a MediaRecorder interaction in the composer; only the resulting upload reaches the server. |
| `attachments/recording-a-voice-message/composer-draft-preserved` | Frontend unit / component | Recording is a MediaRecorder interaction in the composer; only the resulting upload reaches the server. |
| `attachments/voice-attachment-playback/playing-a-voice-attachment` | Frontend unit / component | Playback and seeking are audio-element behaviour with no server involvement beyond the download. |
| `attachments/voice-attachment-playback/seeking-within-a-voice-attachment` | Frontend unit / component | Playback and seeking are audio-element behaviour with no server involvement beyond the download. |
| `attachments/voice-attachment-playback/audio-that-cannot-be-prepared-for-playback` | Frontend unit / component | Playback and seeking are audio-element behaviour with no server involvement beyond the download. |

## authentication

12 of 17 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `authentication/sign-in-with-email-and-password/valid-credentials` | Backend integration | Credential verification and the response to repeated failures are decided by Identity on the server. |
| `authentication/sign-in-with-email-and-password/invalid-credentials` | Backend integration | Credential verification and the response to repeated failures are decided by Identity on the server. |
| `authentication/sign-in-with-email-and-password/repeated-failures` | Backend integration | Credential verification and the response to repeated failures are decided by Identity on the server. |
| `authentication/bearer-token-authorization/missing-token` | Backend integration | Whether an endpoint requires a token, and which is the public exception, is server-enforced. |
| `authentication/bearer-token-authorization/public-catalog-exception` | Backend integration | Whether an endpoint requires a token, and which is the public exception, is server-enforced. |
| `authentication/transparent-token-refresh/expired-access-token` | Frontend unit / component | Detecting a 401, sharing one in-flight refresh, retrying, and clearing credentials on failure are all client responsibilities in apiFetch. |
| `authentication/transparent-token-refresh/refresh-rejected` | Frontend unit / component | Detecting a 401, sharing one in-flight refresh, retrying, and clearing credentials on failure are all client responsibilities in apiFetch. |
| `authentication/persistent-login/keep-me-signed-in` | End-to-end | Whether credentials survive closing the browser is only observable by actually closing and reopening one. |
| `authentication/persistent-login/not-persisted` | End-to-end | Whether credentials survive closing the browser is only observable by actually closing and reopening one. |
| `authentication/persistent-login/no-idle-logout` | Frontend unit / component | The absence of an inactivity timer is a client property; the server never expires a session for idleness. |
| `authentication/sign-out-affects-only-the-current-browser/signing-out-one-browser` | Backend integration | That signing out revokes one session and leaves the account's other sessions usable is server state. |
| `authentication/password-change/successful-change` | Backend integration | Current-password verification and the new password's policy are enforced by Identity. |
| `authentication/password-change/wrong-current-password` | Backend integration | Current-password verification and the new password's policy are enforced by Identity. |
| `authentication/password-change/weak-new-password` | Backend integration | Current-password verification and the new password's policy are enforced by Identity. |
| `authentication/password-reset-by-email/requesting-a-reset` | Backend integration | Issuing, accepting, and refusing a reset code is server-side. |
| `authentication/password-reset-by-email/completing-a-reset` | Backend integration | Issuing, accepting, and refusing a reset code is server-side. |
| `authentication/password-reset-by-email/invalid-reset-code` | Backend integration | Issuing, accepting, and refusing a reset code is server-side. |

## automated-testing

0 of 21 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `automated-testing/layered-verification/server-enforced-rule` | Verified by the tooling itself | The test machinery is its own subject: satisfied by the layers existing and by this triage document. |
| `automated-testing/layered-verification/client-enforced-rule` | Verified by the tooling itself | The test machinery is its own subject: satisfied by the layers existing and by this triage document. |
| `automated-testing/layered-verification/cross-cutting-flow` | Verified by the tooling itself | The test machinery is its own subject: satisfied by the layers existing and by this triage document. |
| `automated-testing/layered-verification/real-time-behavior-at-the-integration-layer` | Verified by the tooling itself | The test machinery is its own subject: satisfied by the layers existing and by this triage document. |
| `automated-testing/scenario-traceability/covered-scenario` | Verified by the tooling itself | Verified by tools/spec-coverage's own behaviour on each run, not by an application test. |
| `automated-testing/scenario-traceability/uncovered-scenario` | Verified by the tooling itself | Verified by tools/spec-coverage's own behaviour on each run, not by an application test. |
| `automated-testing/scenario-traceability/test-claiming-an-unknown-scenario` | Verified by the tooling itself | Verified by tools/spec-coverage's own behaviour on each run, not by an application test. |
| `automated-testing/scenario-traceability/specification-changes` | Verified by the tooling itself | Verified by tools/spec-coverage's own behaviour on each run, not by an application test. |
| `automated-testing/scenario-traceability/failing-test-does-not-count` | Verified by the tooling itself | Verified by tools/spec-coverage's own behaviour on each run, not by an application test. |
| `automated-testing/code-coverage-gate/coverage-below-threshold` | Verified by the tooling itself | Verified by tools/coverage-gate's own behaviour, not by an application test. |
| `automated-testing/code-coverage-gate/coverage-at-or-above-threshold` | Verified by the tooling itself | Verified by tools/coverage-gate's own behaviour, not by an application test. |
| `automated-testing/code-coverage-gate/exclusions-are-declared` | Verified by the tooling itself | Verified by tools/coverage-gate's own behaviour, not by an application test. |
| `automated-testing/test-isolation/independent-runs` | Verified by the tooling itself | Verified by running the suite — repeated runs, single-test runs, an interrupted run, an absent database — not by a test inside it. |
| `automated-testing/test-isolation/development-data-untouched` | Verified by the tooling itself | Verified by running the suite — repeated runs, single-test runs, an interrupted run, an absent database — not by a test inside it. |
| `automated-testing/test-isolation/order-independence` | Verified by the tooling itself | Verified by running the suite — repeated runs, single-test runs, an interrupted run, an absent database — not by a test inside it. |
| `automated-testing/test-isolation/aborted-run` | Verified by the tooling itself | Verified by running the suite — repeated runs, single-test runs, an interrupted run, an absent database — not by a test inside it. |
| `automated-testing/test-isolation/database-unavailable` | Verified by the tooling itself | Verified by running the suite — repeated runs, single-test runs, an interrupted run, an absent database — not by a test inside it. |
| `automated-testing/running-the-suites/full-run` | Verified by the tooling itself | Verified by invoking test.bat and each layer's own command. |
| `automated-testing/running-the-suites/single-layer` | Verified by the tooling itself | Verified by invoking test.bat and each layer's own command. |
| `automated-testing/running-the-suites/missing-prerequisite` | Verified by the tooling itself | Verified by invoking test.bat and each layer's own command. |
| `automated-testing/running-the-suites/test-failure-reporting` | Verified by the tooling itself | Verified by invoking test.bat and each layer's own command. |

## chat-rooms

23 of 26 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `chat-rooms/room-creation/creating-a-room` | Backend integration | Creation and its required fields are validated server-side. |
| `chat-rooms/room-creation/missing-name` | Backend integration | Creation and its required fields are validated server-side. |
| `chat-rooms/room-name-uniqueness/duplicate-name-on-creation` | Backend integration | Uniqueness — including whether a deleted room's name frees up — is a database-backed server rule. |
| `chat-rooms/room-name-uniqueness/duplicate-name-on-rename` | Backend integration | Uniqueness — including whether a deleted room's name frees up — is a database-backed server rule. |
| `chat-rooms/room-name-uniqueness/reusing-a-deleted-room-s-name` | Backend integration | Uniqueness — including whether a deleted room's name frees up — is a database-backed server rule. |
| `chat-rooms/room-properties/reading-a-room` | Backend integration | Which room fields are returned, and to whom, is decided by the server. |
| `chat-rooms/room-properties/non-member-reads-a-public-room` | Backend integration | Which room fields are returned, and to whom, is decided by the server. |
| `chat-rooms/public-room-catalog/browsing-the-catalog` | Backend integration | Catalog contents, search, and the exclusion of private rooms are query results the server produces. |
| `chat-rooms/public-room-catalog/searching` | Backend integration | Catalog contents, search, and the exclusion of private rooms are query results the server produces. |
| `chat-rooms/public-room-catalog/private-rooms-hidden` | Backend integration | Catalog contents, search, and the exclusion of private rooms are query results the server produces. |
| `chat-rooms/public-room-catalog/already-a-member` | Backend integration | Catalog contents, search, and the exclusion of private rooms are query results the server produces. |
| `chat-rooms/joining-public-rooms/joining` | Backend integration | Every join outcome — accepted, banned, already a member, private — is a server decision. |
| `chat-rooms/joining-public-rooms/banned-from-the-room` | Backend integration | Every join outcome — accepted, banned, already a member, private — is a server decision. |
| `chat-rooms/joining-public-rooms/already-a-member` | Backend integration | Every join outcome — accepted, banned, already a member, private — is a server decision. |
| `chat-rooms/joining-public-rooms/private-room-join-attempt` | Backend integration | Every join outcome — accepted, banned, already a member, private — is a server decision. |
| `chat-rooms/private-room-access/non-member-requests-a-private-room` | Backend integration | Refusing a non-member's request for a private room is server-enforced. |
| `chat-rooms/private-room-access/private-rooms-screen` | Frontend unit / component | What the private rooms screen lists and how it is reached is a view concern. |
| `chat-rooms/leaving-rooms/member-leaves` | Backend integration | Whether a leave succeeds, and the owner's inability to leave, is enforced on the server. |
| `chat-rooms/leaving-rooms/owner-attempts-to-leave` | Backend integration | Whether a leave succeeds, and the owner's inability to leave, is enforced on the server. |
| `chat-rooms/leaving-rooms/leave-control-visibility` | Frontend unit / component | Whether the Leave control is offered is decided in the component before any call is made. |
| `chat-rooms/room-settings-maintenance/owner-saves-settings` | Backend integration | Owner-only settings authority is re-checked on the update request. |
| `chat-rooms/room-settings-maintenance/admin-attempts-to-change-settings` | Backend integration | Owner-only settings authority is re-checked on the update request. |
| `chat-rooms/room-deletion/owner-deletes-a-room` | Backend integration | Deletion authority, and the room becoming unusable afterwards, is server state. |
| `chat-rooms/room-deletion/member-is-viewing-the-deleted-room` | End-to-end | One user's deletion navigating another user's open browser away is only demonstrable with two live sessions. |
| `chat-rooms/room-deletion/non-owner-attempts-deletion` | Backend integration | Deletion authority, and the room becoming unusable afterwards, is server state. |
| `chat-rooms/room-deletion/messaging-a-deleted-room` | Backend integration | Deletion authority, and the room becoming unusable afterwards, is server state. |

## chat-ui-shell

0 of 27 scenarios sit at the backend integration layer.

This capability has no `BE/Tests.Integration/ChatUiShell/` area — the only one of the fourteen
with none. Two of the rows below look server-shaped and are not: route guarding and the root
redirect are decided by the router before any request leaves the browser, so a backend test
there could observe nothing at all, and the API's own refusal of a caller with no token is a
real server rule that `authentication/bearer-token-authorization` already claims. A backend
area here would have produced either tests asserting the absence of a request or a second
claim on a scenario that is already covered.

| Scenario | Layer | Why |
|---|---|---|
| `chat-ui-shell/application-layout/viewing-a-room` | Frontend unit / component | Which regions the shell renders for a room versus a direct chat is pure composition. |
| `chat-ui-shell/application-layout/viewing-a-direct-chat` | Frontend unit / component | Which regions the shell renders for a room versus a direct chat is pure composition. |
| `chat-ui-shell/top-navigation/navigating` | Frontend unit / component | Navigation targets and the account menu are client routing and rendering. |
| `chat-ui-shell/top-navigation/signing-out-from-the-menu` | End-to-end | Sign-out from the menu returning the browser to the sign-in screen spans the whole stack. |
| `chat-ui-shell/sidebar-with-accordion-sections/compaction-on-entering-a-room` | Frontend unit / component | Accordion compaction and expansion is component state. |
| `chat-ui-shell/sidebar-with-accordion-sections/leaving-the-chat-view` | Frontend unit / component | Accordion compaction and expansion is component state. |
| `chat-ui-shell/sidebar-with-accordion-sections/manual-expansion` | Frontend unit / component | Accordion compaction and expansion is component state. |
| `chat-ui-shell/members-and-context-panel/panel-contents` | Frontend unit / component | What the panel shows and which actions it offers is rendering; the server re-checks the actions themselves under room-moderation. |
| `chat-ui-shell/members-and-context-panel/admin-actions` | Frontend unit / component | What the panel shows and which actions it offers is rendering; the server re-checks the actions themselves under room-moderation. |
| `chat-ui-shell/message-list-scrolling-behavior/at-the-bottom` | Frontend unit / component | Stick-to-bottom, preserved position, and load-on-reaching-top are all list behaviour. |
| `chat-ui-shell/message-list-scrolling-behavior/scrolled-up` | Frontend unit / component | Stick-to-bottom, preserved position, and load-on-reaching-top are all list behaviour. |
| `chat-ui-shell/message-list-scrolling-behavior/reaching-the-top` | Frontend unit / component | Stick-to-bottom, preserved position, and load-on-reaching-top are all list behaviour. |
| `chat-ui-shell/message-composition-controls/multiline-entry` | Frontend unit / component | Key handling, emoji insertion, and draft retention happen before anything is sent. |
| `chat-ui-shell/message-composition-controls/sending-with-enter` | Frontend unit / component | Key handling, emoji insertion, and draft retention happen before anything is sent. |
| `chat-ui-shell/message-composition-controls/inserting-an-emoji` | Frontend unit / component | Key handling, emoji insertion, and draft retention happen before anything is sent. |
| `chat-ui-shell/message-composition-controls/draft-retention` | Frontend unit / component | Key handling, emoji insertion, and draft retention happen before anything is sent. |
| `chat-ui-shell/message-row-affordances/own-message` | Frontend unit / component | Which controls a row offers, and inline edit, are rendering; the authority behind them is covered under messaging. |
| `chat-ui-shell/message-row-affordances/someone-else-s-message` | Frontend unit / component | Which controls a row offers, and inline edit, are rendering; the authority behind them is covered under messaging. |
| `chat-ui-shell/message-row-affordances/inline-edit` | Frontend unit / component | Which controls a row offers, and inline edit, are rendering; the authority behind them is covered under messaging. |
| `chat-ui-shell/administration-through-modal-dialogs/opening-room-management` | Frontend unit / component | Dialog structure, member filtering, and destructive-action confirmation are view behaviour. |
| `chat-ui-shell/administration-through-modal-dialogs/searching-members` | Frontend unit / component | Dialog structure, member filtering, and destructive-action confirmation are view behaviour. |
| `chat-ui-shell/administration-through-modal-dialogs/confirming-a-destructive-action` | Frontend unit / component | Dialog structure, member filtering, and destructive-action confirmation are view behaviour. |
| `chat-ui-shell/unauthenticated-access-and-routing/reaching-a-protected-screen-while-signed-out` | Frontend unit / component | Route guarding and the root redirect are decided by the router before any request is made; the API's own refusal is covered by authentication/bearer-token-authorization. |
| `chat-ui-shell/unauthenticated-access-and-routing/root-redirect` | Frontend unit / component | Route guarding and the root redirect are decided by the router before any request is made; the API's own refusal is covered by authentication/bearer-token-authorization. |
| `chat-ui-shell/loading-and-error-feedback/data-loading` | Frontend unit / component | Skeletons, error toasts, and the missing-room state are rendering of states the server merely reports. |
| `chat-ui-shell/loading-and-error-feedback/failed-action` | Frontend unit / component | Skeletons, error toasts, and the missing-room state are rendering of states the server merely reports. |
| `chat-ui-shell/loading-and-error-feedback/missing-room` | Frontend unit / component | Skeletons, error toasts, and the missing-room state are rendering of states the server merely reports. |

## contacts-and-blocking

17 of 17 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `contacts-and-blocking/personal-contact-list/viewing-contacts` | Backend integration | What the contacts endpoint returns, and that outgoing requests are not among them, is server state. |
| `contacts-and-blocking/personal-contact-list/outgoing-requests-are-not-listed-as-contacts` | Backend integration | What the contacts endpoint returns, and that outgoing requests are not among them, is server state. |
| `contacts-and-blocking/sending-a-friend-request/request-by-username` | Backend integration | Target resolution and every refusal — unknown, duplicate, self — is decided on the server. |
| `contacts-and-blocking/sending-a-friend-request/request-with-a-message` | Backend integration | Target resolution and every refusal — unknown, duplicate, self — is decided on the server. |
| `contacts-and-blocking/sending-a-friend-request/unknown-target` | Backend integration | Target resolution and every refusal — unknown, duplicate, self — is decided on the server. |
| `contacts-and-blocking/sending-a-friend-request/duplicate-request` | Backend integration | Target resolution and every refusal — unknown, duplicate, self — is decided on the server. |
| `contacts-and-blocking/sending-a-friend-request/self-request` | Backend integration | Target resolution and every refusal — unknown, duplicate, self — is decided on the server. |
| `contacts-and-blocking/confirming-friendship/accepting` | Backend integration | Who may accept or reject a request is server-enforced. |
| `contacts-and-blocking/confirming-friendship/rejecting` | Backend integration | Who may accept or reject a request is server-enforced. |
| `contacts-and-blocking/confirming-friendship/sender-cannot-self-accept` | Backend integration | Who may accept or reject a request is server-enforced. |
| `contacts-and-blocking/removing-a-contact/removing-a-friend` | Backend integration | Removal is persisted and symmetric; the server owns it. |
| `contacts-and-blocking/blocking-a-user/blocking-from-the-contact-list` | Backend integration | The block and its consequences for writing and reading are enforced on every subsequent request. |
| `contacts-and-blocking/blocking-a-user/blocked-user-attempts-to-write` | Backend integration | The block and its consequences for writing and reading are enforced on every subsequent request. |
| `contacts-and-blocking/blocking-a-user/history-remains-readable` | Backend integration | The block and its consequences for writing and reading are enforced on every subsequent request. |
| `contacts-and-blocking/blocking-a-user/blocking-oneself` | Backend integration | The block and its consequences for writing and reading are enforced on every subsequent request. |
| `contacts-and-blocking/unblocking/unblocking` | Backend integration | Unblocking and the no-op case are server state transitions. |
| `contacts-and-blocking/unblocking/unblocking-someone-who-is-not-blocked` | Backend integration | Unblocking and the no-op case are server state transitions. |

## direct-messages

11 of 12 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `direct-messages/direct-chats-are-two-participant-chats/feature-parity` | Backend integration | That a direct chat supports the same operations as a room, and never appears in the catalog, is observable on the API. |
| `direct-messages/direct-chats-are-two-participant-chats/no-moderation-roles` | Frontend unit / component | Whether role and management affordances are offered for a direct chat is rendering. |
| `direct-messages/direct-chats-are-two-participant-chats/not-in-the-catalog` | Backend integration | That a direct chat supports the same operations as a room, and never appears in the catalog, is observable on the API. |
| `direct-messages/opening-a-direct-chat-requires-friendship/messaging-a-friend-for-the-first-time` | Backend integration | The friendship precondition, reopening rather than duplicating, and every refusal are server decisions. |
| `direct-messages/opening-a-direct-chat-requires-friendship/reopening-an-existing-chat` | Backend integration | The friendship precondition, reopening rather than duplicating, and every refusal are server decisions. |
| `direct-messages/opening-a-direct-chat-requires-friendship/not-friends` | Backend integration | The friendship precondition, reopening rather than duplicating, and every refusal are server decisions. |
| `direct-messages/opening-a-direct-chat-requires-friendship/blocked-in-either-direction` | Backend integration | The friendship precondition, reopening rather than duplicating, and every refusal are server decisions. |
| `direct-messages/opening-a-direct-chat-requires-friendship/chat-with-self` | Backend integration | The friendship precondition, reopening rather than duplicating, and every refusal are server decisions. |
| `direct-messages/frozen-direct-chats/sending-in-a-frozen-chat` | Backend integration | Freezing on block, allowing reads, and thawing on unblock are enforced per request. |
| `direct-messages/frozen-direct-chats/reading-a-frozen-chat` | Backend integration | Freezing on block, allowing reads, and thawing on unblock are enforced per request. |
| `direct-messages/frozen-direct-chats/unblocking-restores-messaging` | Backend integration | Freezing on block, allowing reads, and thawing on unblock are enforced per request. |
| `direct-messages/direct-chats-in-the-sidebar/listing-direct-chats` | Backend integration | That a user's direct chats are returned alongside their rooms is a server listing. |

## messaging

22 of 25 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `messaging/sending-messages/member-sends-text` | Backend integration | Membership and ban checks on send, and the exact text stored, are server-side. |
| `messaging/sending-messages/multiline-and-emoji` | Backend integration | Membership and ban checks on send, and the exact text stored, are server-side. |
| `messaging/sending-messages/non-member-sends` | Backend integration | Membership and ban checks on send, and the exact text stored, are server-side. |
| `messaging/sending-messages/banned-member-sends` | Backend integration | Membership and ban checks on send, and the exact text stored, are server-side. |
| `messaging/message-size-limit/oversized-send` | Backend integration | The length ceiling is refused by the server. |
| `messaging/message-size-limit/client-side-warning` | Frontend unit / component | The counter and the disabled send button appear before anything is sent. |
| `messaging/replies/composing-a-reply` | Backend integration | Which reply targets are accepted, and what a reply carries, is validated and persisted server-side. |
| `messaging/replies/quoting-a-deleted-message` | Backend integration | Which reply targets are accepted, and what a reply carries, is validated and persisted server-side. |
| `messaging/replies/quoting-an-attachment-message` | Backend integration | Which reply targets are accepted, and what a reply carries, is validated and persisted server-side. |
| `messaging/replies/reply-target-in-another-room` | Backend integration | Which reply targets are accepted, and what a reply carries, is validated and persisted server-side. |
| `messaging/replies/cancelling-a-reply` | Frontend unit / component | Dismissing the reply banner is composer state. |
| `messaging/editing-messages/author-edits` | Backend integration | Edit authority and the refusal to edit a deleted message are enforced on the server. |
| `messaging/editing-messages/non-author-edits` | Backend integration | Edit authority and the refusal to edit a deleted message are enforced on the server. |
| `messaging/editing-messages/editing-a-deleted-message` | Backend integration | Edit authority and the refusal to edit a deleted message are enforced on the server. |
| `messaging/deleting-messages/author-deletes` | Backend integration | Author and admin delete authority, and the idempotence of a second delete, are server rules. |
| `messaging/deleting-messages/admin-deletes-another-user-s-message` | Backend integration | Author and admin delete authority, and the idempotence of a second delete, are server rules. |
| `messaging/deleting-messages/unauthorized-delete` | Backend integration | Author and admin delete authority, and the idempotence of a second delete, are server rules. |
| `messaging/deleting-messages/deleting-twice` | Backend integration | Author and admin delete authority, and the idempotence of a second delete, are server rules. |
| `messaging/persistent-history-in-chronological-order/reading-history` | Backend integration | Ordering, durability, and what an absent recipient sees on their next read are database-backed. |
| `messaging/persistent-history-in-chronological-order/offline-recipient` | Backend integration | Ordering, durability, and what an absent recipient sees on their next read are database-backed. |
| `messaging/persistent-history-in-chronological-order/history-across-restarts` | Backend integration | Ordering, durability, and what an absent recipient sees on their next read are database-backed. |
| `messaging/incremental-history-loading/scrolling-back` | Backend integration | Paging by cursor and signalling the beginning of history are server query behaviour. |
| `messaging/incremental-history-loading/reaching-the-beginning` | Backend integration | Paging by cursor and signalling the beginning of history are server query behaviour. |
| `messaging/incremental-history-loading/large-history-remains-usable` | Load and capacity (not built) | A 10,000-message room staying responsive is a capacity property, and the load layer is not built. |
| `messaging/history-access-control/non-member-requests-history` | Backend integration | Refusing a non-member's history request is server-enforced. |

## platform-constraints

5 of 10 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `platform-constraints/capacity/concurrent-users` | Load and capacity (not built) | 300 concurrent users and a 1000-member room are capacity claims; the load layer is specified but not built. |
| `platform-constraints/capacity/large-room` | Load and capacity (not built) | 300 concurrent users and a 1000-member room are capacity claims; the load layer is specified but not built. |
| `platform-constraints/performance-targets/very-large-history` | Load and capacity (not built) | Behaviour at 10,000 messages is a capacity claim; the load layer is not built. |
| `platform-constraints/durable-persistence/restart` | Backend integration | Surviving a host restart, and migrations running on startup, are observable by restarting the application the tests host. |
| `platform-constraints/durable-persistence/startup-migration` | Backend integration | Surviving a host restart, and migrations running on startup, are observable by restarting the application the tests host. |
| `platform-constraints/file-storage/configured-root` | Backend integration | That content is written beneath the configured root, and served from there, is checkable on disk after an upload. |
| `platform-constraints/consistency-of-access-decisions/ban-takes-effect-immediately` | Backend integration | That a ban or a promotion binds on the very next call is precisely a server-side, per-request evaluation. |
| `platform-constraints/consistency-of-access-decisions/promotion-takes-effect-immediately` | Backend integration | That a ban or a promotion binds on the very next call is precisely a server-side, per-request evaluation. |
| `platform-constraints/client-server-transport-boundary/proxied-call` | End-to-end | Whether the browser only ever calls relative paths through its own proxy is visible only from the browser. |
| `platform-constraints/deployment-topology/bringing-the-stack-up` | End-to-end | Which services are published and which are internal is a property of the running compose stack. |

## realtime-delivery

8 of 16 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `realtime-delivery/single-authenticated-realtime-connection/connection-after-sign-in` | Backend integration | Whether the hub accepts a connection, and on what credential, is server-enforced. |
| `realtime-delivery/single-authenticated-realtime-connection/sign-out` | Frontend unit / component | Stopping the connection on sign-out is the client's side of the lifecycle. |
| `realtime-delivery/single-authenticated-realtime-connection/unauthenticated-connect` | Backend integration | Whether the hub accepts a connection, and on what credential, is server-enforced. |
| `realtime-delivery/automatic-reconnection/transient-network-loss` | Frontend unit / component | The retry schedule after a dropped connection is client policy. |
| `realtime-delivery/automatic-reconnection/presence-after-reconnect` | End-to-end | Presence converging again after a real network interruption needs the assembled stack. |
| `realtime-delivery/recipient-scoping/room-message-fan-out` | Backend integration | Which connections a server-pushed event reaches is exactly what the hub's group enrolment decides. |
| `realtime-delivery/recipient-scoping/joining-mid-session` | Backend integration | Which connections a server-pushed event reaches is exactly what the hub's group enrolment decides. |
| `realtime-delivery/recipient-scoping/personal-notification` | Backend integration | Which connections a server-pushed event reaches is exactly what the hub's group enrolment decides. |
| `realtime-delivery/recipient-scoping/subscriptions-on-connect` | Backend integration | Which connections a server-pushed event reaches is exactly what the hub's group enrolment decides. |
| `realtime-delivery/pushed-event-kinds/edit-is-applied-in-place` | Frontend unit / component | The scenarios are about applying an arriving event to the open view; that the server emits it is covered by recipient-scoping. |
| `realtime-delivery/pushed-event-kinds/deletion-is-applied-in-place` | Frontend unit / component | The scenarios are about applying an arriving event to the open view; that the server emits it is covered by recipient-scoping. |
| `realtime-delivery/pushed-event-kinds/membership-change-refreshes-derived-views` | Frontend unit / component | The scenarios are about applying an arriving event to the open view; that the server emits it is covered by recipient-scoping. |
| `realtime-delivery/pushed-event-kinds/room-deletion-while-viewing` | End-to-end | Being navigated out of a room another user deleted is a whole-stack flow. |
| `realtime-delivery/delivery-latency/message-delivery` | Backend integration | The three-second budget from accepted send to a second client's receipt is measurable with two hub clients. |
| `realtime-delivery/duplicate-suppression/own-message-echo` | Frontend unit / component | Reconciling the local insert against the echoed event by id happens in the client cache. |
| `realtime-delivery/operation-failures-surface-as-errors/refused-send` | Backend integration | That a refused hub invocation returns an error carrying the reason is the server's contract. |

## room-moderation

28 of 29 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `room-moderation/room-roles/owner-privileges-are-permanent` | Backend integration | That the owner's authority cannot be taken away is enforced on every moderation call. |
| `room-moderation/room-roles/role-display` | Frontend unit / component | How owner and admin are labelled in the member list is rendering. |
| `room-moderation/moderation-requires-admin-authority/member-attempts-moderation` | Backend integration | Refusing a member's or an outsider's moderation call is the definition of a server-enforced rule. |
| `room-moderation/moderation-requires-admin-authority/outsider-attempts-moderation` | Backend integration | Refusing a member's or an outsider's moderation call is the definition of a server-enforced rule. |
| `room-moderation/managing-admins/promoting-a-member` | Backend integration | Promotion, demotion, and their edge cases are persisted role changes. |
| `room-moderation/managing-admins/demoting-an-admin` | Backend integration | Promotion, demotion, and their edge cases are persisted role changes. |
| `room-moderation/managing-admins/promoting-an-existing-admin` | Backend integration | Promotion, demotion, and their edge cases are persisted role changes. |
| `room-moderation/managing-admins/target-is-not-a-member` | Backend integration | Promotion, demotion, and their edge cases are persisted role changes. |
| `room-moderation/removal-is-a-ban/removing-a-member` | Backend integration | That removal also bans, and what that means for a rejoin attempt, is server state. |
| `room-moderation/removal-is-a-ban/removed-user-tries-to-rejoin` | Backend integration | That removal also bans, and what that means for a rejoin attempt, is server state. |
| `room-moderation/removal-is-a-ban/admin-removing-an-admin` | Backend integration | That removal also bans, and what that means for a rejoin attempt, is server state. |
| `room-moderation/banning-users/banning-a-member-with-a-reason` | Backend integration | Ban authority, the recorded reason, and the repeat case are server decisions. |
| `room-moderation/banning-users/banning-an-already-banned-user` | Backend integration | Ban authority, the recorded reason, and the repeat case are server decisions. |
| `room-moderation/banning-users/admin-banning-an-admin` | Backend integration | Ban authority, the recorded reason, and the repeat case are server decisions. |
| `room-moderation/ban-list-visibility-and-unbanning/viewing-bans` | Backend integration | Who may read the ban list, and the effect of unbanning, is server-enforced. |
| `room-moderation/ban-list-visibility-and-unbanning/unbanning` | Backend integration | Who may read the ban list, and the effect of unbanning, is server-enforced. |
| `room-moderation/ban-list-visibility-and-unbanning/unbanning-someone-not-banned` | Backend integration | Who may read the ban list, and the effect of unbanning, is server-enforced. |
| `room-moderation/loss-of-room-access/reading-history-after-removal` | Backend integration | Refusing history and attachment downloads to a removed user is re-evaluated per request. |
| `room-moderation/loss-of-room-access/downloading-an-attachment-after-removal` | Backend integration | Refusing history and attachment downloads to a removed user is re-evaluated per request. |
| `room-moderation/room-invitations/sending-an-invitation` | Backend integration | Who may invite whom, and the duplicate and banned cases, is decided on the server. |
| `room-moderation/room-invitations/inviting-a-banned-user` | Backend integration | Who may invite whom, and the duplicate and banned cases, is decided on the server. |
| `room-moderation/room-invitations/duplicate-invitation` | Backend integration | Who may invite whom, and the duplicate and banned cases, is decided on the server. |
| `room-moderation/room-invitations/non-admin-attempts-to-invite` | Backend integration | Who may invite whom, and the duplicate and banned cases, is decided on the server. |
| `room-moderation/responding-to-invitations/accepting-an-invitation` | Backend integration | Who may answer an invitation, and what each answer does, is server state. |
| `room-moderation/responding-to-invitations/rejecting-an-invitation` | Backend integration | Who may answer an invitation, and what each answer does, is server state. |
| `room-moderation/responding-to-invitations/answering-someone-else-s-invitation` | Backend integration | Who may answer an invitation, and what each answer does, is server state. |
| `room-moderation/responding-to-invitations/answering-twice` | Backend integration | Who may answer an invitation, and what each answer does, is server state. |
| `room-moderation/responding-to-invitations/invitation-to-a-deleted-room` | Backend integration | Who may answer an invitation, and what each answer does, is server state. |
| `room-moderation/responding-to-invitations/banned-before-accepting` | Backend integration | Who may answer an invitation, and what each answer does, is server state. |

## speech-to-text

5 of 9 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `speech-to-text/transcribing-an-audio-clip/successful-transcription` | Backend integration | The transcription endpoint's authorization and its handling of bad audio and an unavailable service are server behaviour. |
| `speech-to-text/transcribing-an-audio-clip/unauthenticated-request` | Backend integration | The transcription endpoint's authorization and its handling of bad audio and an unavailable service are server behaviour. |
| `speech-to-text/transcribing-an-audio-clip/empty-or-unreadable-audio` | Backend integration | The transcription endpoint's authorization and its handling of bad audio and an unavailable service are server behaviour. |
| `speech-to-text/transcribing-an-audio-clip/transcription-service-unavailable` | Backend integration | The transcription endpoint's authorization and its handling of bad audio and an unavailable service are server behaviour. |
| `speech-to-text/dictate-into-the-composer/dictating-a-message` | Frontend unit / component | Recording, inserting the result at the caret, and handling a denied microphone are composer behaviour. |
| `speech-to-text/dictate-into-the-composer/dictation-with-existing-composer-text` | Frontend unit / component | Recording, inserting the result at the caret, and handling a denied microphone are composer behaviour. |
| `speech-to-text/dictate-into-the-composer/recording-permission-denied` | Frontend unit / component | Recording, inserting the result at the caret, and handling a denied microphone are composer behaviour. |
| `speech-to-text/dictation-length-limit/recording-reaches-the-limit` | Frontend unit / component | Stopping the recorder at the limit is client-side. |
| `speech-to-text/dictation-length-limit/oversized-clip-submitted-directly` | Backend integration | A client that ignores the limit is refused by the server. |

## unread-notifications

7 of 13 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `unread-notifications/unread-indicators/message-arrives-in-a-background-chat` | Backend integration | The unread count itself is incremented and pushed by the server. |
| `unread-notifications/unread-indicators/message-arrives-in-the-open-chat` | Frontend unit / component | Whether a badge is raised, and the aggregate shown, is client state; the server does not know which chat is open. |
| `unread-notifications/unread-indicators/aggregate-count` | Frontend unit / component | Whether a badge is raised, and the aggregate shown, is client state; the server does not know which chat is open. |
| `unread-notifications/clearing-unread-state/opening-a-chat-clears-the-badge` | Backend integration | The read position is persisted against the membership and clears the server-side count. |
| `unread-notifications/clearing-unread-state/read-position-recorded-per-membership` | Backend integration | The read position is persisted against the membership and clears the server-side count. |
| `unread-notifications/clearing-unread-state/real-time-channel-unavailable` | Frontend unit / component | Choosing the HTTP fallback when the hub is down is a client decision. |
| `unread-notifications/clearing-unread-state/marking-read-in-a-chat-one-is-not-in` | Backend integration | The read position is persisted against the membership and clears the server-side count. |
| `unread-notifications/typing-indicators/composing` | Backend integration | Who receives a typing notice, and that the composer does not receive their own, is the hub's fan-out. |
| `unread-notifications/typing-indicators/sending-stops-typing` | Backend integration | Who receives a typing notice, and that the composer does not receive their own, is the hub's fan-out. |
| `unread-notifications/friend-request-notification/receiving-a-request-while-online` | Backend integration | The push to the recipient's personal group is server-side; the list refresh it triggers is covered under contacts. |
| `unread-notifications/invitation-visibility/invitation-arrives` | Frontend unit / component | The sidebar section, its count, and its refresh interval are view behaviour over an endpoint covered by room-moderation. |
| `unread-notifications/invitation-visibility/no-pending-invitations` | Frontend unit / component | The sidebar section, its count, and its refresh interval are view behaviour over an endpoint covered by room-moderation. |
| `unread-notifications/invitation-visibility/answering-an-invitation-updates-lists` | Frontend unit / component | The sidebar section, its count, and its refresh interval are view behaviour over an endpoint covered by room-moderation. |

## user-accounts

13 of 13 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `user-accounts/self-registration/successful-registration` | Backend integration | Registration and its required fields are validated by Identity. |
| `user-accounts/self-registration/missing-field` | Backend integration | Registration and its required fields are validated by Identity. |
| `user-accounts/email-and-username-uniqueness/duplicate-username` | Backend integration | Uniqueness is a database-backed server rule. |
| `user-accounts/email-and-username-uniqueness/duplicate-email` | Backend integration | Uniqueness is a database-backed server rule. |
| `user-accounts/username-format-and-immutability/invalid-username-characters` | Backend integration | The accepted character set, the email-shaped rejection, and the absence of a rename endpoint are all server-side. |
| `user-accounts/username-format-and-immutability/username-equals-email` | Backend integration | The accepted character set, the email-shaped rejection, and the absence of a rename endpoint are all server-side. |
| `user-accounts/username-format-and-immutability/no-rename-path` | Backend integration | The accepted character set, the email-shaped rejection, and the absence of a rename endpoint are all server-side. |
| `user-accounts/display-name/setting-a-display-name` | Backend integration | The stored display name, and what is returned when there is none, is server state. |
| `user-accounts/display-name/no-display-name` | Backend integration | The stored display name, and what is returned when there is none, is server state. |
| `user-accounts/own-profile-retrieval/reading-own-profile` | Backend integration | What the profile endpoint returns, and its refusal without a token, is server behaviour. |
| `user-accounts/own-profile-retrieval/unauthenticated-profile-request` | Backend integration | What the profile endpoint returns, and its refusal without a token, is server behaviour. |
| `user-accounts/account-deletion/deleting-an-account` | Backend integration | Deletion and the state of a second attempt are server-side. |
| `user-accounts/account-deletion/deleting-twice` | Backend integration | Deletion and the state of a second attempt are server-side. |

## user-presence

11 of 15 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `user-presence/presence-states/connected-and-active` | Backend integration | The server classifies each user into exactly one of the three states and answers lookups with it. |
| `user-presence/presence-states/connected-but-idle` | Backend integration | The server classifies each user into exactly one of the three states and answers lookups with it. |
| `user-presence/presence-states/no-connection` | Backend integration | The server classifies each user into exactly one of the three states and answers lookups with it. |
| `user-presence/activity-heartbeat-and-idle-threshold/going-idle` | Backend integration | The 60-second window is applied by the server to the heartbeats it receives. |
| `user-presence/activity-heartbeat-and-idle-threshold/returning-from-idle` | Backend integration | The 60-second window is applied by the server to the heartbeats it receives. |
| `user-presence/activity-heartbeat-and-idle-threshold/hidden-tab` | Frontend unit / component | Deciding to report inactive because the tab is hidden happens in the client before the heartbeat. |
| `user-presence/multi-tab-presence/two-tabs-open` | Backend integration | That a user stays online while any connection remains, and goes offline when the last one ends, is the server's aggregation. |
| `user-presence/multi-tab-presence/leader-tab-closes` | Frontend unit / component | Heartbeat leadership and its transfer between tabs is entirely client-side. |
| `user-presence/multi-tab-presence/closing-the-last-tab` | Backend integration | That a user stays online while any connection remains, and goes offline when the last one ends, is the server's aggregation. |
| `user-presence/presence-change-broadcast/contact-comes-online` | Backend integration | Pushing a transition to observing connections on connect and disconnect is the hub's job. |
| `user-presence/presence-change-broadcast/contact-disconnects` | Backend integration | Pushing a transition to observing connections on connect and disconnect is the hub's job. |
| `user-presence/presence-lookup-on-demand/hydrating-a-freshly-opened-view` | Backend integration | The bulk lookup is a hub invocation answered from server state. |
| `user-presence/presence-lookup-on-demand/after-reconnect` | Backend integration | The bulk lookup is a hub invocation answered from server state. |
| `user-presence/presence-indicators-in-the-ui/member-list-indicators` | Frontend unit / component | How a state — including an unknown one — is drawn next to a name is rendering. |
| `user-presence/presence-indicators-in-the-ui/unknown-presence` | Frontend unit / component | How a state — including an unknown one — is drawn next to a name is rendering. |

## user-sessions

5 of 8 scenarios sit at the backend integration layer.

| Scenario | Layer | Why |
|---|---|---|
| `user-sessions/session-registration-per-browser/new-sign-in-registers-a-session` | Backend integration | Already covered at the backend layer. |
| `user-sessions/session-registration-per-browser/restored-login-without-a-session` | Frontend unit / component | Already covered. Registering on a restored login, and tolerating a failed registration, are client responsibilities. |
| `user-sessions/session-registration-per-browser/session-registration-failure-is-non-fatal` | Frontend unit / component | Already covered. Registering on a restored login, and tolerating a failed registration, are client responsibilities. |
| `user-sessions/listing-active-sessions/viewing-sessions` | Backend integration | Already covered at the backend layer. |
| `user-sessions/listing-active-sessions/only-own-sessions` | Backend integration | Already covered at the backend layer. |
| `user-sessions/revoking-sessions/revoking-another-session` | Backend integration | Already covered at the backend layer. |
| `user-sessions/revoking-sessions/revoking-the-current-session` | End-to-end | Already covered end-to-end: revoking the session the browser is using returns it to sign-in. |
| `user-sessions/revoking-sessions/revoking-a-foreign-session` | Backend integration | Already covered at the backend layer. |
