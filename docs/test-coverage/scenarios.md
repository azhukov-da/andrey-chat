# Scenario coverage

Generated by `tools/spec-coverage` from `openspec/specs/**/spec.md` and the machine-readable
results of the three gated test layers. Do not edit by hand — the next run overwrites it.

Generated: 2026-08-19T20:30:26.299Z

**181 of 291 specified scenarios covered (62.2%).**

Scenario coverage is tracked separately from line coverage and does not gate the run; it says
what is left to test, not how thoroughly the tested code is exercised.

## Layers in this report

| Layer | Result file | Tests | Claiming a scenario |
|---|---|---|---|
| backend integration | `C:\Projects\andrey-chat\BE\Tests.Integration\TestResults\integration.trx` | 238 | 233 |
| frontend unit | `C:\Projects\andrey-chat\docs\test-coverage\raw\vitest.json` | 6 | 6 |
| end-to-end | `C:\Projects\andrey-chat\docs\test-coverage\raw\playwright.json` | 2 | 2 |

## Per capability

| Capability | Covered | Total | % |
|---|---:|---:|---:|
| [attachments](#attachments) | 17 | 33 | 52% |
| [authentication](#authentication) | 11 | 17 | 65% |
| [automated-testing](#automated-testing) | 0 | 21 | 0% |
| [chat-rooms](#chat-rooms) | 22 | 26 | 85% |
| [chat-ui-shell](#chat-ui-shell) | 0 | 27 | 0% |
| [contacts-and-blocking](#contacts-and-blocking) | 17 | 17 | 100% |
| [direct-messages](#direct-messages) | 11 | 12 | 92% |
| [messaging](#messaging) | 21 | 25 | 84% |
| [platform-constraints](#platform-constraints) | 5 | 10 | 50% |
| [realtime-delivery](#realtime-delivery) | 8 | 16 | 50% |
| [room-moderation](#room-moderation) | 28 | 29 | 97% |
| [speech-to-text](#speech-to-text) | 4 | 9 | 44% |
| [unread-notifications](#unread-notifications) | 6 | 13 | 46% |
| [user-accounts](#user-accounts) | 13 | 13 | 100% |
| [user-presence](#user-presence) | 10 | 15 | 67% |
| [user-sessions](#user-sessions) | 8 | 8 | 100% |
| **total** | **181** | **291** | **62.2%** |

## attachments

17 of 33 scenarios covered (52%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `uploading-a-file` — Uploading a file | backend | backend integration: a member's upload is stored and becomes a message carrying the file's metadata<br>backend integration: the upload is pushed to the room's other participants as it happens |
| `uploading-several-files-at-once` — Uploading several files at once | backend | backend integration: each of several files becomes its own message |
| `non-member-upload` — Non-member upload | backend | backend integration: an upload into a deleted room is refused<br>backend integration: an upload by someone who is not a member is refused as not-a-member |
| `banned-uploader` — Banned uploader | backend | backend integration: an upload by a banned user is refused, whatever the reason given |
| `empty-file` — Empty file | backend | backend integration: a zero-length file is rejected as a bad request<br>backend integration: a request carrying no file at all is rejected as a bad request |
| `oversized-image` — Oversized image | backend | backend integration: an image over 3 MB is refused with an error naming the image limit<br>backend integration: an image just inside 3 MB is accepted |
| `oversized-file` — Oversized file | backend | backend integration: the limit that applies is chosen by kind, so a 4 MB non-image is accepted<br>backend integration: a non-image over 20 MB is refused with an error naming the file limit |
| `original-name-preserved` — Original name preserved | backend | backend integration: an attachment keeps the name it was uploaded under, not its storage name |
| `comment-on-an-attachment` — Comment on an attachment | backend | backend integration: a comment supplied with an upload is stored on the attachment and becomes the message text<br>backend integration: an upload with no comment carries none, rather than an empty one |
| `member-downloads` — Member downloads | backend | backend integration: a member gets the bytes back with the original name and content type |
| `non-member-downloads` — Non-member downloads | backend | backend integration: someone who is not a member of the containing chat is refused as not-a-member |
| `uploader-who-lost-access` — Uploader who lost access | backend | backend integration: the uploader is refused their own file once they leave the room<br>backend integration: the uploader is refused their own file once they are banned from the room |
| `unknown-attachment` — Unknown attachment | backend | backend integration: an attachment id that does not exist returns not found<br>backend integration: an attachment whose stored content has gone returns not found rather than an empty file |
| `file-survives-loss-of-access` — File survives loss of access | backend | backend integration: the file stays in storage and stays readable by the room when the uploader loses access |
| `storage-layout` — Storage layout | backend | backend integration: the bytes are written beneath the room's own directory in the configured uploads root |
| `voice-attachment-gets-a-transcript` — Voice attachment gets a transcript | backend | backend integration: only audio is submitted for transcription<br>backend integration: the transcript is added alongside the uploader's comment rather than replacing it<br>backend integration: an audio upload is a message at once, and gains a transcript caption when transcription completes |
| `transcription-fails-for-a-voice-attachment` — Transcription fails for a voice attachment | backend | backend integration: a failed transcription leaves the attachment intact and uncaptioned, and tells nobody<br>backend integration: a transcription that succeeds with no words leaves no caption |

### Claimed by a test that did not pass

- `attachments/transcription-eligibility-length-limit/long-voice-attachment-uploads-without-a-transcript` — Long voice attachment uploads without a transcript — backend integration: an audio attachment longer than ten minutes is not submitted for transcription (skipped)

### Uncovered

- `attachments/upload-by-paste/pasting-an-image` — **Upload by paste** → Pasting an image
- `attachments/attachment-metadata/metadata-display` — **Attachment metadata** → Metadata display
- `attachments/attachment-metadata/voice-message-metadata-display` — **Attachment metadata** → Voice message metadata display
- `attachments/inline-image-rendering/image-preview` — **Inline image rendering** → Image preview
- `attachments/inline-image-rendering/preview-fails` — **Inline image rendering** → Preview fails
- `attachments/inline-image-rendering/other-file-kinds` — **Inline image rendering** → Other file kinds
- `attachments/voice-attachment-transcription/transcript-shown-alongside-playback` — **Voice attachment transcription** → Transcript shown alongside playback
- `attachments/recording-a-voice-message/recording-and-sending-a-voice-message` — **Recording a voice message** → Recording and sending a voice message
- `attachments/recording-a-voice-message/recording-in-progress` — **Recording a voice message** → Recording in progress
- `attachments/recording-a-voice-message/recording-reaches-the-length-limit` — **Recording a voice message** → Recording reaches the length limit
- `attachments/recording-a-voice-message/microphone-unavailable-while-recording` — **Recording a voice message** → Microphone unavailable while recording
- `attachments/recording-a-voice-message/composer-draft-preserved` — **Recording a voice message** → Composer draft preserved
- `attachments/voice-attachment-playback/playing-a-voice-attachment` — **Voice attachment playback** → Playing a voice attachment
- `attachments/voice-attachment-playback/seeking-within-a-voice-attachment` — **Voice attachment playback** → Seeking within a voice attachment
- `attachments/voice-attachment-playback/audio-that-cannot-be-prepared-for-playback` — **Voice attachment playback** → Audio that cannot be prepared for playback

## authentication

11 of 17 scenarios covered (65%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `valid-credentials` — Valid credentials | backend | backend integration: a correct email and password return an access token, a refresh token, and a lifetime<br>backend integration: a username may be used in place of the email address |
| `invalid-credentials` — Invalid credentials | backend | backend integration: an unknown email or a wrong password fails as unauthorized and issues no tokens(unknownAccount: True)<br>backend integration: an unknown email or a wrong password fails as unauthorized and issues no tokens(unknownAccount: False) |
| `repeated-failures` — Repeated failures | backend | backend integration: repeated wrong passwords count toward lockout and eventually lock the account |
| `missing-token` — Missing token | backend | backend integration: an invalid access token is rejected the same way a missing one is<br>backend integration: a protected endpoint called without an access token is rejected with 401(path: "/api/me/invitations")<br>backend integration: a protected endpoint called without an access token is rejected with 401(path: "/api/Rooms/mine")<br>backend integration: a protected endpoint called without an access token is rejected with 401(path: "/api/Me")<br>backend integration: a protected endpoint called without an access token is rejected with 401(path: "/api/Friends")<br>backend integration: a protected endpoint called without an access token is rejected with 401(path: "/api/Sessions") |
| `public-catalog-exception` — Public catalog exception | backend | backend integration: the public room catalog answers an unauthenticated caller, with no membership fields set |
| `signing-out-one-browser` — Signing out one browser | backend | backend integration: signing out revokes only the calling browser's session; the other stays usable |
| `successful-change` — Successful change | backend | backend integration: the correct current password and a valid new one change it, and the new one signs in<br>backend integration: a password is stored only as a hash, never in recoverable form |
| `wrong-current-password` — Wrong current password | backend | backend integration: a wrong current password is refused and leaves the stored password alone |
| `weak-new-password` — Weak new password | backend | backend integration: a new password failing the policy is refused with the unmet requirement stated(weak: "Ab1", requirement: "minimum length")<br>backend integration: a new password failing the policy is refused with the unmet requirement stated(weak: "NoDigitsHere", requirement: "a digit")<br>backend integration: a new password failing the policy is refused with the unmet requirement stated(weak: "alllowercase1", requirement: "an uppercase letter") |
| `requesting-a-reset` — Requesting a reset | backend | backend integration: a reset request answers the same way whether or not the address is registered |
| `invalid-reset-code` — Invalid reset code | backend | backend integration: an unrecognised reset code is refused and leaves the password unchanged |

### Claimed by a test that did not pass

- `authentication/password-reset-by-email/completing-a-reset` — Completing a reset — backend integration: a valid reset code sets the new password, and the visitor signs in with it (skipped)

### Uncovered

- `authentication/transparent-token-refresh/expired-access-token` — **Transparent token refresh** → Expired access token
- `authentication/transparent-token-refresh/refresh-rejected` — **Transparent token refresh** → Refresh rejected
- `authentication/persistent-login/keep-me-signed-in` — **Persistent login** → Keep me signed in
- `authentication/persistent-login/not-persisted` — **Persistent login** → Not persisted
- `authentication/persistent-login/no-idle-logout` — **Persistent login** → No idle logout

## automated-testing

0 of 21 scenarios covered (0%).

### Uncovered

- `automated-testing/layered-verification/server-enforced-rule` — **Layered verification** → Server-enforced rule
- `automated-testing/layered-verification/client-enforced-rule` — **Layered verification** → Client-enforced rule
- `automated-testing/layered-verification/cross-cutting-flow` — **Layered verification** → Cross-cutting flow
- `automated-testing/layered-verification/real-time-behavior-at-the-integration-layer` — **Layered verification** → Real-time behavior at the integration layer
- `automated-testing/scenario-traceability/covered-scenario` — **Scenario traceability** → Covered scenario
- `automated-testing/scenario-traceability/uncovered-scenario` — **Scenario traceability** → Uncovered scenario
- `automated-testing/scenario-traceability/test-claiming-an-unknown-scenario` — **Scenario traceability** → Test claiming an unknown scenario
- `automated-testing/scenario-traceability/specification-changes` — **Scenario traceability** → Specification changes
- `automated-testing/scenario-traceability/failing-test-does-not-count` — **Scenario traceability** → Failing test does not count
- `automated-testing/code-coverage-gate/coverage-below-threshold` — **Code coverage gate** → Coverage below threshold
- `automated-testing/code-coverage-gate/coverage-at-or-above-threshold` — **Code coverage gate** → Coverage at or above threshold
- `automated-testing/code-coverage-gate/exclusions-are-declared` — **Code coverage gate** → Exclusions are declared
- `automated-testing/test-isolation/independent-runs` — **Test isolation** → Independent runs
- `automated-testing/test-isolation/development-data-untouched` — **Test isolation** → Development data untouched
- `automated-testing/test-isolation/order-independence` — **Test isolation** → Order independence
- `automated-testing/test-isolation/aborted-run` — **Test isolation** → Aborted run
- `automated-testing/test-isolation/database-unavailable` — **Test isolation** → Database unavailable
- `automated-testing/running-the-suites/full-run` — **Running the suites** → Full run
- `automated-testing/running-the-suites/single-layer` — **Running the suites** → Single layer
- `automated-testing/running-the-suites/missing-prerequisite` — **Running the suites** → Missing prerequisite
- `automated-testing/running-the-suites/test-failure-reporting` — **Running the suites** → Test failure reporting

## chat-rooms

22 of 26 scenarios covered (85%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `creating-a-room` — Creating a room | backend | backend integration: the creator becomes owner of a room carrying the name, description, and visibility given |
| `duplicate-name-on-creation` — Duplicate name on creation | backend | backend integration: a room name already in use is refused at creation |
| `duplicate-name-on-rename` — Duplicate name on rename | backend | backend integration: renaming onto an existing room's name is refused and the room keeps its name |
| `reusing-a-deleted-room-s-name` — Reusing a deleted room's name | backend | backend integration: a deleted room's name is free for a new room |
| `reading-a-room` — Reading a room | backend | backend integration: a member's read carries name, description, visibility, owner, member count, and their role |
| `non-member-reads-a-public-room` — Non-member reads a public room | backend | backend integration: a non-member reads a public room and gets no role of their own |
| `browsing-the-catalog` — Browsing the catalog | backend | backend integration: the catalog lists public rooms with description and member count, and pages |
| `searching` — Searching | backend | backend integration: search matches name or description, case-insensitively, and excludes the rest |
| `private-rooms-hidden` — Private rooms hidden | backend | backend integration: a private room is absent from the catalog even for its own members |
| `already-a-member` — Already a member | backend | backend integration: a catalog entry the caller already belongs to carries their role, so it can offer Open |
| `joining` — Joining | backend | backend integration: joining a public room makes the caller a Member and puts the room in their list |
| `banned-from-the-room` — Banned from the room | backend | backend integration: a banned user's join is rejected with a banned error |
| `already-a-member` — Already a member | backend | backend integration: joining a room the caller already belongs to is rejected as already a member |
| `private-room-join-attempt` — Private room join attempt | backend | backend integration: joining a private room directly is rejected; access needs an invitation |
| `non-member-requests-a-private-room` — Non-member requests a private room | backend | backend integration: a non-member requesting a private room is refused with a private-room error |
| `member-leaves` — Member leaves | backend | backend integration: a member leaves; the room drops out of their list and the member count falls |
| `owner-attempts-to-leave` — Owner attempts to leave | backend | backend integration: the owner cannot leave their own room |
| `owner-saves-settings` — Owner saves settings | backend | backend integration: the owner saves a new name, description, and visibility |
| `admin-attempts-to-change-settings` — Admin attempts to change settings | backend | backend integration: an admin who is not the owner is refused, and the settings are unchanged |
| `owner-deletes-a-room` — Owner deletes a room | backend | backend integration: the owner deletes a room; it is marked deleted and no longer readable |
| `non-owner-attempts-deletion` — Non-owner attempts deletion | backend | backend integration: a member's delete is refused and the room survives |
| `messaging-a-deleted-room` — Messaging a deleted room | backend | backend integration: posting to a deleted room is refused as already deleted |

### Claimed by a test that did not pass

- `chat-rooms/room-creation/missing-name` — Missing name — backend integration: a room with no usable name is refused and nothing is created (skipped)

### Uncovered

- `chat-rooms/private-room-access/private-rooms-screen` — **Private room access** → Private rooms screen
- `chat-rooms/leaving-rooms/leave-control-visibility` — **Leaving rooms** → Leave control visibility
- `chat-rooms/room-deletion/member-is-viewing-the-deleted-room` — **Room deletion** → Member is viewing the deleted room

## chat-ui-shell

0 of 27 scenarios covered (0%).

### Uncovered

- `chat-ui-shell/application-layout/viewing-a-room` — **Application layout** → Viewing a room
- `chat-ui-shell/application-layout/viewing-a-direct-chat` — **Application layout** → Viewing a direct chat
- `chat-ui-shell/top-navigation/navigating` — **Top navigation** → Navigating
- `chat-ui-shell/top-navigation/signing-out-from-the-menu` — **Top navigation** → Signing out from the menu
- `chat-ui-shell/sidebar-with-accordion-sections/compaction-on-entering-a-room` — **Sidebar with accordion sections** → Compaction on entering a room
- `chat-ui-shell/sidebar-with-accordion-sections/leaving-the-chat-view` — **Sidebar with accordion sections** → Leaving the chat view
- `chat-ui-shell/sidebar-with-accordion-sections/manual-expansion` — **Sidebar with accordion sections** → Manual expansion
- `chat-ui-shell/members-and-context-panel/panel-contents` — **Members and context panel** → Panel contents
- `chat-ui-shell/members-and-context-panel/admin-actions` — **Members and context panel** → Admin actions
- `chat-ui-shell/message-list-scrolling-behavior/at-the-bottom` — **Message list scrolling behavior** → At the bottom
- `chat-ui-shell/message-list-scrolling-behavior/scrolled-up` — **Message list scrolling behavior** → Scrolled up
- `chat-ui-shell/message-list-scrolling-behavior/reaching-the-top` — **Message list scrolling behavior** → Reaching the top
- `chat-ui-shell/message-composition-controls/multiline-entry` — **Message composition controls** → Multiline entry
- `chat-ui-shell/message-composition-controls/sending-with-enter` — **Message composition controls** → Sending with Enter
- `chat-ui-shell/message-composition-controls/inserting-an-emoji` — **Message composition controls** → Inserting an emoji
- `chat-ui-shell/message-composition-controls/draft-retention` — **Message composition controls** → Draft retention
- `chat-ui-shell/message-row-affordances/own-message` — **Message row affordances** → Own message
- `chat-ui-shell/message-row-affordances/someone-else-s-message` — **Message row affordances** → Someone else's message
- `chat-ui-shell/message-row-affordances/inline-edit` — **Message row affordances** → Inline edit
- `chat-ui-shell/administration-through-modal-dialogs/opening-room-management` — **Administration through modal dialogs** → Opening room management
- `chat-ui-shell/administration-through-modal-dialogs/searching-members` — **Administration through modal dialogs** → Searching members
- `chat-ui-shell/administration-through-modal-dialogs/confirming-a-destructive-action` — **Administration through modal dialogs** → Confirming a destructive action
- `chat-ui-shell/unauthenticated-access-and-routing/reaching-a-protected-screen-while-signed-out` — **Unauthenticated access and routing** → Reaching a protected screen while signed out
- `chat-ui-shell/unauthenticated-access-and-routing/root-redirect` — **Unauthenticated access and routing** → Root redirect
- `chat-ui-shell/loading-and-error-feedback/data-loading` — **Loading and error feedback** → Data loading
- `chat-ui-shell/loading-and-error-feedback/failed-action` — **Loading and error feedback** → Failed action
- `chat-ui-shell/loading-and-error-feedback/missing-room` — **Loading and error feedback** → Missing room

## contacts-and-blocking

17 of 17 scenarios covered (100%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `viewing-contacts` — Viewing contacts | backend | backend integration: the list carries confirmed friends and incoming pending requests, each distinguishable |
| `outgoing-requests-are-not-listed-as-contacts` — Outgoing requests are not listed as contacts | backend | backend integration: an unanswered outgoing request does not appear in the sender's list |
| `request-by-username` — Request by username | backend | backend integration: a request by username creates a pending friendship recorded against the sender |
| `request-with-a-message` — Request with a message | backend | backend integration: the optional message is stored with the request |
| `unknown-target` — Unknown target | backend | backend integration: a request naming no account is rejected as user-not-found |
| `duplicate-request` — Duplicate request | backend | backend integration: a second request, or a request between existing friends, is rejected |
| `self-request` — Self request | backend | backend integration: a request to oneself is rejected |
| `accepting` — Accepting | backend | backend integration: accepting records the acceptance time and puts each user in the other's list |
| `rejecting` — Rejecting | backend | backend integration: rejecting removes the request and leaves neither user in the other's list |
| `sender-cannot-self-accept` — Sender cannot self-accept | backend | backend integration: the requesting user can neither accept nor reject their own request |
| `removing-a-friend` — Removing a friend | backend | backend integration: removal deletes the friendship and empties it from both lists |
| `blocking-from-the-contact-list` — Blocking from the contact list | backend | backend integration: blocking freezes the direct chat and refuses further messages from either side<br>backend integration: blocking someone already blocked is rejected |
| `blocked-user-attempts-to-write` — Blocked user attempts to write | backend | backend integration: the blocked user's send in the existing chat is refused as forbidden |
| `history-remains-readable` — History remains readable | backend | backend integration: both users can still read the frozen chat's history |
| `blocking-oneself` — Blocking oneself | backend | backend integration: blocking one's own account is rejected |
| `unblocking` — Unblocking | backend | backend integration: unblocking unfreezes the chat and messaging works again between friends |
| `unblocking-someone-who-is-not-blocked` — Unblocking someone who is not blocked | backend | backend integration: unblocking someone who is not blocked is rejected |

## direct-messages

11 of 12 scenarios covered (92%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `feature-parity` — Feature parity | backend | backend integration: a direct chat supports text, replies, attachments, and history as a room does |
| `not-in-the-catalog` — Not in the catalog | backend | backend integration: no direct chat appears in the public catalog |
| `messaging-a-friend-for-the-first-time` — Messaging a friend for the first time | backend | backend integration: a first message to a confirmed contact creates a two-member direct chat |
| `reopening-an-existing-chat` — Reopening an existing chat | backend | backend integration: opening again returns the same chat with its history, rather than a second one |
| `not-friends` — Not friends | backend | backend integration: opening a chat with someone who is not a confirmed friend is refused |
| `blocked-in-either-direction` — Blocked in either direction | backend | backend integration: a block in either direction refuses a new chat(blockerOpens: False)<br>backend integration: a block in either direction refuses a new chat(blockerOpens: True) |
| `chat-with-self` — Chat with self | backend | backend integration: opening a chat with one's own username is refused |
| `sending-in-a-frozen-chat` — Sending in a frozen chat | backend | backend integration: sending in a frozen chat is refused for both participants |
| `reading-a-frozen-chat` — Reading a frozen chat | backend | backend integration: a frozen chat can still be opened and read by both participants |
| `unblocking-restores-messaging` — Unblocking restores messaging | backend | backend integration: unblocking thaws the chat and both sides can write again |
| `listing-direct-chats` — Listing direct chats | backend | backend integration: a direct chat is listed alongside the user's rooms, for both participants |

### Uncovered

- `direct-messages/direct-chats-are-two-participant-chats/no-moderation-roles` — **Direct chats are two-participant chats** → No moderation roles

## messaging

21 of 25 scenarios covered (84%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `member-sends-text` — Member sends text | backend | backend integration: a member's message is persisted with its author and creation time |
| `multiline-and-emoji` — Multiline and emoji | backend | backend integration: line breaks and emoji survive the round trip byte for byte |
| `non-member-sends` — Non-member sends | backend | backend integration: a non-member's post is refused as not-a-member and stores nothing |
| `oversized-send` — Oversized send | backend | backend integration: text over 3072 UTF-8 bytes is refused as too large and nothing is stored<br>backend integration: the limit counts UTF-8 bytes, not characters |
| `composing-a-reply` — Composing a reply | backend | backend integration: a reply records the message it answers, and history carries the reference |
| `quoting-a-deleted-message` — Quoting a deleted message | backend | backend integration: a reply to a message that is later deleted keeps its reference, and the target reads as deleted |
| `quoting-an-attachment-message` — Quoting an attachment message | backend | backend integration: a reply to an attachment message with no text can name the file, because history carries it |
| `reply-target-in-another-room` — Reply target in another room | backend | backend integration: a reply naming a message from another room is refused as message-not-found |
| `author-edits` — Author edits | backend | backend integration: the author's edit updates the text and records the edit time |
| `non-author-edits` — Non-author edits | backend | backend integration: someone else's edit is refused, even the room owner's, and the text is untouched |
| `editing-a-deleted-message` — Editing a deleted message | backend | backend integration: an edit of a deleted message is refused as already deleted |
| `author-deletes` — Author deletes | backend | backend integration: the author's delete marks the message deleted |
| `admin-deletes-another-user-s-message` — Admin deletes another user's message | backend | backend integration: a room owner or admin deletes someone else's message |
| `unauthorized-delete` — Unauthorized delete | backend | backend integration: a plain member deleting someone else's message is refused |
| `deleting-twice` — Deleting twice | backend | backend integration: deleting an already deleted message is refused as already deleted |
| `reading-history` — Reading history | backend | backend integration: a page carries the most recent messages in a total order, with author, time, and attachments |
| `offline-recipient` — Offline recipient | backend | backend integration: messages sent while a member was away are in their history when they next read it |
| `history-across-restarts` — History across restarts | backend | backend integration: messages survive a restart of the application |
| `scrolling-back` — Scrolling back | backend | backend integration: the cursor walks back through older pages without gaps or repeats |
| `reaching-the-beginning` — Reaching the beginning | backend | backend integration: an empty chat reports no messages and no more to load<br>backend integration: the page that reaches the start of history reports that nothing older remains |
| `non-member-requests-history` — Non-member requests history | backend | backend integration: a non-member's history request is refused as not-a-member |

### Claimed by a test that did not pass

- `messaging/sending-messages/banned-member-sends` — Banned member sends — backend integration: a banned user's post is refused as banned (skipped)

### Uncovered

- `messaging/message-size-limit/client-side-warning` — **Message size limit** → Client-side warning
- `messaging/replies/cancelling-a-reply` — **Replies** → Cancelling a reply
- `messaging/incremental-history-loading/large-history-remains-usable` — **Incremental history loading** → Large history remains usable

## platform-constraints

5 of 10 scenarios covered (50%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `restart` — Restart | backend | backend integration: a signed-in user's data is still reachable over HTTP through a restarted host<br>backend integration: messages, memberships, roles, bans and invitations are all intact after the host restarts |
| `startup-migration` — Startup migration | backend | backend integration: starting against a database behind the current schema migrates it before serving traffic |
| `configured-root` — Configured root | backend | backend integration: uploaded content is written beneath the configured uploads root and served back from there |
| `ban-takes-effect-immediately` — Ban takes effect immediately | backend | backend integration: the very next read, post, and download after a ban are all refused |
| `promotion-takes-effect-immediately` — Promotion takes effect immediately | backend | backend integration: the very next moderation call after a promotion is authorized<br>backend integration: a promotion is visible to a session that was already signed in, with no reconnect |

### Uncovered

- `platform-constraints/capacity/concurrent-users` — **Capacity** → Concurrent users
- `platform-constraints/capacity/large-room` — **Capacity** → Large room
- `platform-constraints/performance-targets/very-large-history` — **Performance targets** → Very large history
- `platform-constraints/client-server-transport-boundary/proxied-call` — **Client-server transport boundary** → Proxied call
- `platform-constraints/deployment-topology/bringing-the-stack-up` — **Deployment topology** → Bringing the stack up

## realtime-delivery

8 of 16 scenarios covered (50%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `connection-after-sign-in` — Connection after sign-in | backend | backend integration: a signed-in user's access token establishes a hub connection |
| `unauthenticated-connect` — Unauthenticated connect | backend | backend integration: a connection attempt with no access token is refused |
| `room-message-fan-out` — Room message fan-out | backend | backend integration: a room message reaches every member's connection and nobody else's<br>backend integration: a message sent over the hub fans out the same way one sent over HTTP does |
| `joining-mid-session` — Joining mid-session | backend | backend integration: a user who joins while already connected starts receiving that room's messages |
| `personal-notification` — Personal notification | backend | backend integration: a personal notification reaches only the user it is addressed to |
| `subscriptions-on-connect` — Subscriptions on connect | backend | backend integration: connecting enrols the user in their personal group and every room they belong to |
| `message-delivery` — Message delivery | backend | backend integration: a message reaches a connected recipient well within the three-second budget |
| `refused-send` — Refused send | backend | backend integration: a hub send the server refuses fails with an error carrying the reason<br>backend integration: the hub enforces the same rules as the HTTP API, refusing an oversized send |

### Uncovered

- `realtime-delivery/single-authenticated-realtime-connection/sign-out` — **Single authenticated realtime connection** → Sign-out
- `realtime-delivery/automatic-reconnection/transient-network-loss` — **Automatic reconnection** → Transient network loss
- `realtime-delivery/automatic-reconnection/presence-after-reconnect` — **Automatic reconnection** → Presence after reconnect
- `realtime-delivery/pushed-event-kinds/edit-is-applied-in-place` — **Pushed event kinds** → Edit is applied in place
- `realtime-delivery/pushed-event-kinds/deletion-is-applied-in-place` — **Pushed event kinds** → Deletion is applied in place
- `realtime-delivery/pushed-event-kinds/membership-change-refreshes-derived-views` — **Pushed event kinds** → Membership change refreshes derived views
- `realtime-delivery/pushed-event-kinds/room-deletion-while-viewing` — **Pushed event kinds** → Room deletion while viewing
- `realtime-delivery/duplicate-suppression/own-message-echo` — **Duplicate suppression** → Own message echo

## room-moderation

28 of 29 scenarios covered (97%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `owner-privileges-are-permanent` — Owner privileges are permanent | backend | backend integration: the owner cannot be demoted, banned, or removed, by an admin or by themselves |
| `member-attempts-moderation` — Member attempts moderation | backend | backend integration: a plain member's moderation calls are refused as not-owner-or-admin |
| `outsider-attempts-moderation` — Outsider attempts moderation | backend | backend integration: a non-member's moderation calls are refused |
| `promoting-a-member` — Promoting a member | backend | backend integration: the owner promotes a member and the member list shows the new role |
| `demoting-an-admin` — Demoting an admin | backend | backend integration: revoking admin status returns the user to Member |
| `promoting-an-existing-admin` — Promoting an existing admin | backend | backend integration: promoting someone who is already an admin succeeds and changes nothing |
| `target-is-not-a-member` — Target is not a member | backend | backend integration: a role change aimed at a non-member is refused as not-a-member |
| `removing-a-member` — Removing a member | backend | backend integration: removal drops the membership and records a ban naming the acting admin |
| `removed-user-tries-to-rejoin` — Removed user tries to rejoin | backend | backend integration: the removed user's attempt to rejoin the public room is refused as banned |
| `admin-removing-an-admin` — Admin removing an admin | backend | backend integration: a non-owner admin removing another admin is refused as forbidden |
| `banning-a-member-with-a-reason` — Banning a member with a reason | backend | backend integration: a user who was never a member can be banned in advance<br>backend integration: a ban removes the member and records who, why, and when |
| `banning-an-already-banned-user` — Banning an already banned user | backend | backend integration: banning an already banned user succeeds without a duplicate entry |
| `admin-banning-an-admin` — Admin banning an admin | backend | backend integration: a non-owner admin banning another admin is refused as forbidden |
| `viewing-bans` — Viewing bans | backend | backend integration: an admin reads the ban list with user, banner, timestamp, and reason; a member cannot |
| `unbanning` — Unbanning | backend | backend integration: unbanning removes the entry and lets the user join the public room again |
| `unbanning-someone-not-banned` — Unbanning someone not banned | backend | backend integration: unbanning a user with no ban entry succeeds and changes nothing |
| `reading-history-after-removal` — Reading history after removal | backend | backend integration: a removed user's history request is refused as not-a-member |
| `downloading-an-attachment-after-removal` — Downloading an attachment after removal | backend | backend integration: a removed user cannot download the room's attachments, including their own upload |
| `sending-an-invitation` — Sending an invitation | backend | backend integration: an invitation naming an unknown user, an existing member, or the inviter is refused<br>backend integration: an admin's invitation is created pending and names the room, invitee, and inviter |
| `inviting-a-banned-user` — Inviting a banned user | backend | backend integration: inviting a user banned from the room is refused as banned |
| `duplicate-invitation` — Duplicate invitation | backend | backend integration: a second pending invitation for the same user and room is refused |
| `non-admin-attempts-to-invite` — Non-admin attempts to invite | backend | backend integration: a plain member cannot invite anyone |
| `accepting-an-invitation` — Accepting an invitation | backend | backend integration: accepting makes the invitee a Member and puts the room in their list |
| `rejecting-an-invitation` — Rejecting an invitation | backend | backend integration: rejecting marks it rejected and creates no membership |
| `answering-someone-else-s-invitation` — Answering someone else's invitation | backend | backend integration: anyone other than the invitee is refused as not-the-recipient |
| `answering-twice` — Answering twice | backend | backend integration: answering an invitation that is no longer pending is refused as already processed |
| `invitation-to-a-deleted-room` — Invitation to a deleted room | backend | backend integration: accepting an invitation to a deleted room is refused and creates no membership |
| `banned-before-accepting` — Banned before accepting | backend | backend integration: a ban applied after the invitation was sent refuses the acceptance |

### Uncovered

- `room-moderation/room-roles/role-display` — **Room roles** → Role display

## speech-to-text

4 of 9 scenarios covered (44%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `successful-transcription` — Successful transcription | backend | backend integration: an authenticated user submitting a clip gets its transcript back |
| `unauthenticated-request` — Unauthenticated request | backend | backend integration: a request with no session is refused as unauthorized and never reaches the transcriber |
| `empty-or-unreadable-audio` — Empty or unreadable audio | backend | backend integration: audio the backend cannot decode is a bad request, with no partial transcript<br>backend integration: a zero-length clip is rejected as a bad request without being sent on |
| `transcription-service-unavailable` — Transcription service unavailable | backend | backend integration: a failed transcription leaves the caller's own state untouched<br>backend integration: an unreachable local backend answers as unavailable, distinctly from a bad clip |

### Claimed by a test that did not pass

- `speech-to-text/dictation-length-limit/oversized-clip-submitted-directly` — Oversized clip submitted directly — backend integration: a clip longer than 120 seconds is rejected without transcription being attempted (skipped)

### Uncovered

- `speech-to-text/dictate-into-the-composer/dictating-a-message` — **Dictate into the composer** → Dictating a message
- `speech-to-text/dictate-into-the-composer/dictation-with-existing-composer-text` — **Dictate into the composer** → Dictation with existing composer text
- `speech-to-text/dictate-into-the-composer/recording-permission-denied` — **Dictate into the composer** → Recording permission denied
- `speech-to-text/dictation-length-limit/recording-reaches-the-limit` — **Dictation length limit** → Recording reaches the limit

## unread-notifications

6 of 13 scenarios covered (46%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `opening-a-chat-clears-the-badge` — Opening a chat clears the badge | backend | backend integration: the recorded position survives a reconnect and moves forward as reading continues |
| `read-position-recorded-per-membership` — Read position recorded per membership | backend | backend integration: the read position is stored against the caller's membership of that chat |
| `marking-read-in-a-chat-one-is-not-in` — Marking read in a chat one is not in | backend | backend integration: marking read in a chat the caller does not belong to is refused as not-a-member<br>backend integration: the same refusal applies over the real-time channel |
| `composing` — Composing | backend | backend integration: a typing notice reaches the other participants and not the person typing |
| `sending-stops-typing` — Sending stops typing | backend | backend integration: a stopped-typing notice reaches the other participants and not the sender |
| `receiving-a-request-while-online` — Receiving a request while online | backend | backend integration: a friend request is pushed to the recipient with the requester's identity and message |

### Claimed by a test that did not pass

- `unread-notifications/unread-indicators/message-arrives-in-a-background-chat` — Message arrives in a background chat — backend integration: a message in a chat raises that chat's unread count for the recipient (skipped)

### Uncovered

- `unread-notifications/unread-indicators/message-arrives-in-the-open-chat` — **Unread indicators** → Message arrives in the open chat
- `unread-notifications/unread-indicators/aggregate-count` — **Unread indicators** → Aggregate count
- `unread-notifications/clearing-unread-state/real-time-channel-unavailable` — **Clearing unread state** → Real-time channel unavailable
- `unread-notifications/invitation-visibility/invitation-arrives` — **Invitation visibility** → Invitation arrives
- `unread-notifications/invitation-visibility/no-pending-invitations` — **Invitation visibility** → No pending invitations
- `unread-notifications/invitation-visibility/answering-an-invitation-updates-lists` — **Invitation visibility** → Answering an invitation updates lists

## user-accounts

13 of 13 scenarios covered (100%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `successful-registration` — Successful registration | backend | backend integration: an unused email and username create an account that can sign in immediately |
| `missing-field` — Missing field | backend | backend integration: registration missing a required field is rejected naming that field, and creates nothing(omitted: "username")<br>backend integration: registration missing a required field is rejected naming that field, and creates nothing(omitted: "email")<br>backend integration: registration missing a required field is rejected naming that field, and creates nothing(omitted: "password") |
| `duplicate-username` — Duplicate username | backend | backend integration: a username already in use is rejected, case-insensitively, naming the username |
| `duplicate-email` — Duplicate email | backend | backend integration: an email already in use is rejected, case-insensitively, naming the email |
| `invalid-username-characters` — Invalid username characters | backend | backend integration: a username outside 3-32 letters, digits, dot, underscore, or hyphen is rejected(username: "emoji😀")<br>backend integration: a username outside 3-32 letters, digits, dot, underscore, or hyphen is rejected(username: "ab")<br>backend integration: a username outside 3-32 letters, digits, dot, underscore, or hyphen is rejected(username: "has/slash")<br>backend integration: a username outside 3-32 letters, digits, dot, underscore, or hyphen is rejected(username: "has space")<br>backend integration: a username outside 3-32 letters, digits, dot, underscore, or hyphen is rejected(username: "has@at")<br>backend integration: a username outside 3-32 letters, digits, dot, underscore, or hyphen is rejected(username: "thisusernameislongerthanthirtytwocharacters") |
| `username-equals-email` — Username equals email | backend | backend integration: a username equal to the submitted email, ignoring case, is rejected(differentCase: True)<br>backend integration: a username equal to the submitted email, ignoring case, is rejected(differentCase: False) |
| `no-rename-path` — No rename path | backend | backend integration: no exposed operation changes a username, and setting a display name leaves it alone |
| `setting-a-display-name` — Setting a display name | backend | backend integration: a saved display name reaches message headers, member lists, and contact lists |
| `no-display-name` — No display name | backend | backend integration: a user with no display name still carries their username as the stable handle |
| `reading-own-profile` — Reading own profile | backend | backend integration: the profile carries id, username, display name, email, and creation time |
| `unauthenticated-profile-request` — Unauthenticated profile request | backend | backend integration: a profile request with no credentials is rejected as unauthorized |
| `deleting-an-account` — Deleting an account | backend | backend integration: deletion marks the account deleted and removes the user's attachment files from storage |
| `deleting-twice` — Deleting twice | backend | backend integration: a second delete of an already deleted account is refused as already deleted |

## user-presence

10 of 15 scenarios covered (67%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `connected-and-active` — Connected and active | backend | backend integration: a connection reporting activity puts the user online |
| `no-connection` — No connection | backend | backend integration: a user with no live connection is offline |
| `going-idle` — Going idle | backend | backend integration: a heartbeat reporting inactivity moves the user out of online |
| `returning-from-idle` — Returning from idle | backend | backend integration: activity after a quiet spell puts the user back online |
| `two-tabs-open` — Two tabs open | backend | backend integration: a user with two connections stays online, and stays online when one ends |
| `closing-the-last-tab` — Closing the last tab | backend | backend integration: a user goes offline when their last connection ends |
| `contact-comes-online` — Contact comes online | backend | backend integration: an observer is pushed a presence change when someone connects |
| `contact-disconnects` — Contact disconnects | backend | backend integration: an observer is pushed a presence change when someone's connection drops |
| `hydrating-a-freshly-opened-view` — Hydrating a freshly opened view | backend | backend integration: one call answers the state of a set of users, including those never seen<br>backend integration: a lookup for nobody returns nothing rather than failing |
| `after-reconnect` — After reconnect | backend | backend integration: a lookup after reconnecting returns current state, not what was true before |

### Claimed by a test that did not pass

- `user-presence/presence-states/connected-but-idle` — Connected but idle — backend integration: a connection reporting no activity puts the user in the afk state (skipped)

### Uncovered

- `user-presence/activity-heartbeat-and-idle-threshold/hidden-tab` — **Activity heartbeat and idle threshold** → Hidden tab
- `user-presence/multi-tab-presence/leader-tab-closes` — **Multi-tab presence** → Leader tab closes
- `user-presence/presence-indicators-in-the-ui/member-list-indicators` — **Presence indicators in the UI** → Member list indicators
- `user-presence/presence-indicators-in-the-ui/unknown-presence` — **Presence indicators in the UI** → Unknown presence

## user-sessions

8 of 8 scenarios covered (100%).

### Covered

| Scenario | Layers | Tests |
|---|---|---|
| `new-sign-in-registers-a-session` — New sign-in registers a session | backend, frontend | backend integration: registration records the originating IP address the connection reports<br>backend integration: registration records the device description, user agent, and both timestamps<br>frontend unit: session registration signing in with no stored session registers one and persists its identifier<br>frontend unit: session registration the stored identifier is sent as X-Session-Id on later requests |
| `restored-login-without-a-session` — Restored login without a session | frontend | frontend unit: session registration a restored login with no stored session registers a new one<br>frontend unit: session registration a restored login that already has a session identifier does not register another |
| `session-registration-failure-is-non-fatal` — Session registration failure is non-fatal | frontend | frontend unit: session registration sign-in still completes when session registration fails |
| `viewing-sessions` — Viewing sessions | backend, frontend | backend integration: lists every active session most-recently-seen first, with device details<br>frontend unit: sessions screen lists every session with its device details and marks the current browser "Current" |
| `only-own-sessions` — Only own sessions | backend | backend integration: a user never sees another account's sessions |
| `revoking-another-session` — Revoking another session | backend, e2e | backend integration: revoking marks the session revoked and drops it from the list<br>end-to-end: revoking a session other than the current one removes its row and leaves this browser signed in |
| `revoking-the-current-session` — Revoking the current session | e2e | end-to-end: revoking the session marked Current clears credentials and returns to the sign-in screen |
| `revoking-a-foreign-session` — Revoking a foreign session | backend | backend integration: revoking a session belonging to another account is rejected and revokes nothing |

